{
  // Sensitive settings (connection strings, API keys, secrets) have been removed for public repository.
  // Use appsettings.Development.json (gitignored) for local development secrets.
  "AllowedHosts": "*"
}
